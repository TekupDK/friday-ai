import { describe, it } from "vitest";

// Placeholder suite to keep file active without failing CI
// TODO: Add calendar integration tests when MCP connection is stable
describe.skip("Calendar Integration", () => {
  it("placeholder", () => {
    // Intentionally empty - tests will be added later
  });
});
