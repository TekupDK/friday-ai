# Langfuse Configuration
# Copy to your main .env.dev file

# Enable/disable Langfuse tracing
LANGFUSE_ENABLED=true

# Langfuse API Keys (get from http://localhost:3001 after setup)
LANGFUSE_PUBLIC_KEY=pk-lf-your-public-key-here
LANGFUSE_SECRET_KEY=sk-lf-your-secret-key-here

# Langfuse Server URL (Port 3001 to avoid conflict with Friday AI on 3000)
LANGFUSE_BASE_URL=http://localhost:3001

# Optional: Sampling rate (0.0 to 1.0, default 1.0 = 100%)
# LANGFUSE_SAMPLE_RATE=1.0

# Optional: Flush interval in milliseconds (default 1000)
# LANGFUSE_FLUSH_INTERVAL=1000
